pipeline {
    agent { label 'slave-ubuntu

    stages {
        stage('bulid') {
            steps {
                echo 'buliding done'
                sh "ls"
            }
        }
        stage('test') {
            steps {
                echo 'testing done'
            }
        }
        stage('deploy') {
            steps {
                echo 'deploying done'
            }
        }
    }
}

